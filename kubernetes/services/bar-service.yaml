apiVersion: v1
kind: Service
metadata:
  name: bar-echo-service
  namespace: default
  labels:
    app: bar-echo
    component: http-echo
spec:
  type: ClusterIP
  selector:
    app: bar-echo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  sessionAffinity: None
